Installation
=============

Flask_PyJWT can be installed with ``pip``:

.. code-block:: console

    pip install Flask_PyJWT

Or, with ``poetry``:

.. code-block:: console

    poetry add Flask_PyJWT
